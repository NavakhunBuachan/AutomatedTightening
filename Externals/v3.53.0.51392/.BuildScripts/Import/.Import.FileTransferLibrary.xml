<!--
This file can be imported into any project to add a conditional project reference, 
with the build configuration based on the .NET Framework and platform.

Example usage (update path relative to file containing the Import node):
 <Import Project=".BuildScripts\Import\Import.FileTransferLibrary.xml" Condition="'$(ImportedFileTransferLibrary)' != 'true'" />

Dependency tree (top-down): Fractal Steps -> FileTransferLibrary 

Nested Dependencies: None.

Selection priority:
1. external source code, if '$(ReferenceSourceCode)' == 'true' AND source code exists
2. external binary, if exists. 
   - If $(ReferenceSourceCode), search bottom up; 
   - If not $(ReferenceSourceCode) or not found, search top-down in Externals of project
3. (TODO) internal NuGet server, hosted at http://osatnugetserver.li.lumentuminc.net/nuget 

By using a shared file, all projects are guaranteed to reference the same version,
and if we want to upgrade, only one file needs to be updated.
-->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <!-- Set Imported = true, to use as a condition to prevent multiple nested imports -->
    <ImportedFileTransferLibrary>true</ImportedFileTransferLibrary>
  </PropertyGroup>
  <!-- Reference source code, if it exists, and the configuration is -source; 
  Otherwise, reference External binary files -->
  <Choose>
    <When Condition="'$(ReferenceSourceCode)' == 'true' And Exists('..\FileTransferLibrary\FileTransferLibrary.csproj')">
      <ItemGroup>
        <ProjectReference Include="..\FileTransferLibrary\FileTransferLibrary.csproj">
          <Project>{F21E9A52-5D4B-4C4B-B017-AE3706476776}</Project>
          <Name>FileTransferLibrary</Name>
        </ProjectReference>
      </ItemGroup>
    </When>
    <When Condition="'$(ReferenceSourceCode)' == 'true' And Exists('..\FractalSteps')">
      <ItemGroup>
        <!-- Use the binary output from the FractalSteps project -->
        <Reference Include="FileTransferLibrary">
          <HintPath>..\FractalSteps\Externals\FileTransferLibrary\$(BinSubfolder)\FileTransferLibrary.dll</HintPath>
        </Reference>
      </ItemGroup>
    </When>
    <When Condition="Exists('Externals\FractalSteps\')">
      <ItemGroup>
        <!-- Use the binary from the FractalSteps external of the project -->
        <Reference Include="FileTransferLibrary">
          <HintPath>Externals\FractalSteps\$(BinSubfolder)\FileTransferLibrary.dll</HintPath>
        </Reference>
      </ItemGroup>
    </When>
    <Otherwise>
      <ItemGroup>
        <Reference Include="FileTransferLibrary">
          <HintPath>Externals\FileTransferLibrary\$(BinSubfolder)\FileTransferLibrary.dll</HintPath>
        </Reference>
      </ItemGroup>
    </Otherwise>
  </Choose>
</Project>