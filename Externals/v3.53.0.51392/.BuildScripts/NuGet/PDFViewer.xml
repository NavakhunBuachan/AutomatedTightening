<!-- 
This file can be imported into any project to add a NuGet reference to PdfiumViewer,
with the version based on the .NET Framework.

By using a shared file, all projects are guaranteed to reference the same version, 
and if we want to upgrade, only one file needs to be updated.

Example usage:

  <ImportGroup>
	<Import Project=".BuildScripts\NuGet\PDFViewer.xml" />
  </ImportGroup>
-->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
	<Choose>
		<When Condition="'$(Platform)'=='x64'">
			<ItemGroup>
				<PackageReference Include="PdfiumViewer.Native.x86_64.v8-xfa" Version="2018.4.8.256" />
				<PackageReference Include="PdfiumViewer" Version="2.13.0" />
			</ItemGroup>      
		</When>
		<Otherwise>
			<ItemGroup>
				<PackageReference Include="PdfiumViewer.Native.x86.v8-xfa" Version="2018.4.8.256" />
				<PackageReference Include="PdfiumViewer" Version="2.13.0" />
			</ItemGroup>  
		</Otherwise>
	</Choose>
</Project>