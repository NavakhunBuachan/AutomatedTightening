<!--
This file can be imported into any project to add a conditional project reference to JdsuMeasurementStepsAndProcesses, 
with the build configuration based on the .NET Framework and platform.

Example usage (update path relative to file containing the Import node):
 <Import Project=".BuildScripts\Import\Import.JdsuMeasurementStepsAndProcesses.xml" Condition="'$(ImportedJdsuMeasurementStepsAndProcesses)' != 'true'"/>

Dependency tree (top-down): TODO

Nested Dependencies: FractalSteps, JdsuElectricalTestStation, JdsuOpticalTestStation 

Selection priority:
1. external source code, if '$(ReferenceSourceCode)' == 'true' AND source code exists
2. external binary, if exists. 
   - If $(ReferenceSourceCode), search bottom up; 
   - If not $(ReferenceSourceCode) or not found, search top-down in Externals of project
3. (TODO) internal NuGet server, hosted at http://osatnugetserver.li.lumentuminc.net/nuget 

By using a shared file, all projects are guaranteed to reference the same version,
and if we want to upgrade, only one file needs to be updated.
-->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <!-- Set Imported = true, to use as a condition to prevent multiple nested imports -->
    <ImportedJdsuMeasurementStepsAndProcesses>true</ImportedJdsuMeasurementStepsAndProcesses>
  </PropertyGroup>
  <!-- Reference source code, if it exists, and the configuration is -source; 
  Otherwise, reference External binary files -->
  <Choose>
    <When Condition="'$(ReferenceSourceCode)' == 'true' And Exists('..\JdsuMeasurementStepsAndProcesses\JdsuMeasurementStepsAndProcesses.vbproj')">
      <ItemGroup>
        <ProjectReference Include="..\JdsuMeasurementStepsAndProcesses\JdsuMeasurementStepsAndProcesses.vbproj">
          <Project>{3ED00C82-6A22-46B2-A7B1-11F4BFCA5157}</Project>
          <Name>JdsuMeasurementStepsAndProcesses</Name>
        </ProjectReference>
      </ItemGroup>
    </When>
    <Otherwise>
      <ItemGroup>
        <Reference Include="JdsuMeasurementStepsAndProcesses">
          <HintPath>Externals\JdsuMeasurementStepsAndProcesses\$(BinSubfolder)\JdsuMeasurementStepsAndProcesses.dll</HintPath>
        </Reference>
      </ItemGroup>
    </Otherwise>
  </Choose>
</Project>