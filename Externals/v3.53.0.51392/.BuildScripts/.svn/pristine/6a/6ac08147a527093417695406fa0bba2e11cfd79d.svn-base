<!--
This file can be imported into any project to add a conditional project reference, 
with the build configuration based on the .NET Framework and platform.

Example usage (update path relative to file containing the Import node):
 <Import Project=".BuildScripts\Import\Import.FractalStateMachine.xml" Condition="'$(ImportedFractalStateMachine)' != 'true'"/>

Dependency tree (top-down): FractalSteps - Fractal - FractalStateMachine

Nested Dependencies: FractalLanguage, FractalModel, StationDashboardMessaging

Selection priority:
1. external source code, if '$(ReferenceSourceCode)' == 'true' AND source code exists
2. external binary, if exists. 
   - If $(ReferenceSourceCode), search bottom up; 
   - If not $(ReferenceSourceCode) or not found, search top-down in Externals of project
3. (TODO) internal NuGet server, hosted at http://osatnugetserver.li.lumentuminc.net/nuget 

By using a shared file, all projects are guaranteed to reference the same version,
and if we want to upgrade, only one file needs to be updated.
-->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <!-- Set Imported = true, to use as a condition to prevent multiple nested imports -->
    <ImportedFractalStateMachine>true</ImportedFractalStateMachine>
  </PropertyGroup>
  <!-- Reference source code, if it exists, and the configuration is -source; 
	Otherwise, reference External binary files -->
  <Choose>
    <When Condition="'$(ReferenceSourceCode)' == 'true' And Exists('..\FractalStateMachine')">
      <ItemGroup>
        <ProjectReference Include="..\FractalStateMachine\FractalStateMachine.vbproj">
          <Project>{e9d0d108-e889-4814-9b5f-3a4bedfea61b}</Project>
          <Name>FractalStateMachine</Name>
        </ProjectReference>
      </ItemGroup>
    </When>
    <When Condition="'$(ReferenceSourceCode)' == 'true' And Exists('..\Fractal')">
      <ItemGroup><!-- Use the binary from the FractalStateMachine external of the Fractal project -->
        <Reference Include="FractalStateMachine">
          <HintPath>..\Fractal\Externals\FractalStateMachine\$(BinSubfolder)\FractalStateMachine.dll</HintPath>
        </Reference>
      </ItemGroup>
    </When>
    <When Condition="'$(ReferenceSourceCode)' == 'true' And Exists('..\FractalSteps')">
      <ItemGroup><!-- Use the binary from the Fractal external of the FractalSteps project -->
        <Reference Include="FractalStateMachine">
          <HintPath>..\FractalSteps\Externals\Fractal\$(BinSubfolder)\FractalStateMachine.dll</HintPath>
        </Reference>
      </ItemGroup>
    </When>
    <When Condition="Exists('Externals\FractalSteps\')">
      <ItemGroup><!-- Use the binary from the FractalSteps external of the project -->
        <Reference Include="FractalStateMachine">
          <HintPath>Externals\FractalSteps\$(BinSubfolder)\FractalStateMachine.dll</HintPath>
        </Reference>
      </ItemGroup>
    </When>
    <When Condition="Exists('Externals\Fractal\')">
      <ItemGroup><!-- Use the binary from the Fractal external of the project -->
        <Reference Include="FractalStateMachine">
          <HintPath>Externals\Fractal\$(BinSubfolder)\FractalStateMachine.dll</HintPath>
        </Reference>
      </ItemGroup>
    </When>
    <Otherwise>
      <ItemGroup><!-- Use binary reference included as externals to this project -->
        <Reference Include="FractalStateMachine">
          <HintPath>Externals\FractalStateMachine\$(BinSubfolder)\FractalStateMachine.dll</HintPath>
        </Reference>
      </ItemGroup>
    </Otherwise>
  </Choose>
  <!-- Import nested dependencies -->
  <ImportGroup>
    <Import Project=".Import.FractalLanguage.xml" Condition="'$(ImportedFractalLanguage)' != 'true'"/>
    <Import Project=".Import.FractalModel.xml" Condition="'$(ImportedFractalModel)' != 'true'"/>
    <Import Project=".Import.StationDashboardMessaging.xml" Condition="'$(ImportedStationDashboardMessaging)' != 'true'"/>
  </ImportGroup>
</Project>